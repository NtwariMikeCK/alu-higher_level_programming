#!/usr/bin/node

// Access the first and second arguments passed by the user
const arg1 = process.argv[2];
const arg2 = process.argv[3];

// Print the two arguments in the specified format
console.log(arg1 + ' is ' + arg2);
